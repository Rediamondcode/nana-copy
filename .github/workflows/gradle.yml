name: My First Pipeline

on:
  push:
    branches: [ main ]

jobs:
  hello-job:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        run: echo "Hello, CI/CD World!"

      - name: Print Date
        run: date

      - name: Show Files
        run: ls -la
